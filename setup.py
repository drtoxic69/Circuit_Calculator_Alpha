from setuptools import find_packages, setup, version

setup(
    name="Circuitcal",
    packages=find_packages(),
    version="0.1.0",
    description="This a python library which calculates the current and voltage in the circuit and every single resistor in every combination and gives a pretty step-by-step of the calcution.",
    author="Dr. Toxic",
    license="MIT"
)